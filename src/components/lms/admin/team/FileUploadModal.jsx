/**
 * FileUploadModal — STUB
 *
 * The shared team file library requires a dedicated backend endpoint that has
 * not been built yet. This component is intentionally non-functional until the
 * backend is ready. AdminTeamManager renders a "not available yet" placeholder
 * for the File Library tab so this modal is never triggered.
 *
 * When the backend is ready:
 *  1. Create a POST /team-files endpoint (upload via Cloudinary, store metadata).
 *  2. Replace this stub with the full upload form (see git history for the
 *     original base44 version to port from).
 */
export default function FileUploadModal() {
  return null;
}
