/**
 * TeamFileRow — STUB
 *
 * Renders a single row in the shared team file library. Requires a backend
 * endpoint (POST /team-files, PATCH /team-files/:id) that hasn't been built yet.
 * AdminTeamManager shows a "not available yet" placeholder for the File Library
 * tab so this component is never rendered.
 *
 * When the backend is ready, restore the full implementation from git history
 * and wire it to apiClient instead of the old base44 TeamFile entity.
 */
export default function TeamFileRow() {
  return null;
}
